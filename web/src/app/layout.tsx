import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Sidebar from "@/components/Sidebar";
import { AuthProvider } from "@/lib/auth";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PrithviNet — Smart Environmental Monitoring Platform",
  description:
    "Real-time environmental monitoring, compliance tracking, and predictive analytics for Air, Water & Noise pollution across India.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>
          <Sidebar />
          <main className="min-h-screen bg-[#06080d] lg:pl-[260px]">{children}</main>
        </AuthProvider>
      </body>
    </html>
  );
}
